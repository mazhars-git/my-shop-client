import React from 'react';
import Header from '../Header/Header';
import MainMenu from '../MainMenu/MainMenu';

const Home = () => {
    return (
        <section>
            <MainMenu />
            <Header />
        </section>
    );
};

export default Home;